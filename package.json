{
  "name": "clown-club",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server",
    "build": "echo 'Server only - no build needed'",
    "start": "npm run start --workspace=server"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
